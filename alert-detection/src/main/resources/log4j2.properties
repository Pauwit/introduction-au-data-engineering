# Keep Spark quiet: only warnings and above on the console.
# Spark 3.5 uses Log4j2; this file is picked up automatically from the classpath.
rootLogger.level = warn
rootLogger.appenderRef.stdout.ref = console

appender.console.type = Console
appender.console.name = console
appender.console.target = SYSTEM_ERR
appender.console.layout.type = PatternLayout
appender.console.layout.pattern = %d{yy/MM/dd HH:mm:ss} %p %c{1}: %m%n

# The app's own logs stay visible.
logger.alertdetection.name = alertdetection
logger.alertdetection.level = info

# Silence the two noisiest Spark subsystems even at INFO elsewhere, if ever re-enabled.
logger.streaming.name = org.apache.spark.sql.execution.streaming
logger.streaming.level = warn
logger.statestore.name = org.apache.spark.sql.execution.streaming.state
logger.statestore.level = warn
